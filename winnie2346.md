# Vue Reactivity Project Feedback
**Student:** Winnie (winnie2346)

No Vue project files were submitted — only a `README.md` exists in the repository. There is no code to evaluate.

## Vite CLI – Not Yet
No `package.json` was submitted, so there is no Vite setup to evaluate.

## Iteration in Vue – Not Yet
No Vue files were submitted, so there is no `v-for` to evaluate.

## Data Binding – Not Yet
No Vue files were submitted, so there is no reactive data to evaluate.

## Click Methods – Not Yet
No Vue files were submitted, so there are no click handlers to evaluate.

## Reactive UI – Not Yet
No Vue files were submitted, so there is no reactive UI to evaluate.

## Semantic HTML – Not Yet
No HTML was submitted to evaluate.

## BEM CSS – Not Yet
No CSS was submitted to evaluate.

## Bonus – Aesthetics – Not Yet
No UI was submitted to evaluate.

## Summary of Critical Fixes
- **Submit your project files.** Only a README.md was found — the actual project code needs to be committed and pushed to the repository.
- Make sure your repository includes at minimum: `package.json`, `index.html`, `src/main.js`, and `src/App.vue`.
- To create a new Vue project: run `npm create vite@latest my-project -- --template vue`, then `cd my-project && npm install`.
- Push your files with `git add .`, `git commit -m "add project"`, and `git push`.
